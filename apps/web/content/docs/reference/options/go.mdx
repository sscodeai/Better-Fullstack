---
title: Go Options
description: Go ecosystem option values for frameworks, ORM, API, CLI, logging, auth, services, and workflow flags.
---

Use these values with `--ecosystem go` in CLI commands, MCP payloads, and Stack Builder links.

<Callout kind="info" title="Generated behavior">
Go templates keep API, CLI, logging, auth, libraries, services, and workflow flags separate from TypeScript web options. GoBetterAuth targets Go projects and is documented here.
</Callout>

## Option table

The table below is generated from the canonical option metadata so it stays in sync with the CLI and Stack Builder.

<OptionCategoryTable ecosystem="go" />
