---
title: Rust 選項
description: Rust 生態系中 Web frameworks、frontend、ORM、API、CLI、logging、services 和 workflow flags 的選項值。
---

請在 CLI commands、MCP payloads 和 Stack Builder links 中，將這些值與 `--ecosystem rust` 一起使用。

<Callout kind="info" title="生成行为">
Rust 模板以 Cargo 為先。 Framework、frontend、ORM、API、CLI、logging、error handling、libraries、services 和 workflow flags 都會根據選取的 stack 進行驗證。
</Callout>

## 選項表

下表從規格選項 metadata 生成，因此會與 CLI 和 Stack Builder 保持同步。

<OptionCategoryTable ecosystem="rust" />
