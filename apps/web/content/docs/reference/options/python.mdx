---
title: Python Options
description: Python ecosystem option values for web frameworks, ORM, validation, AI, auth, API, queues, and quality.
---

Use these values with `--ecosystem python` in CLI commands, MCP payloads, and Stack Builder links.

<Callout kind="info" title="Generated behavior">
Python templates use `uv`. Python AI is a Python-specific multi-select and is separate from the TypeScript AI option. Empty conditional files are skipped when selected options do not need them.
</Callout>

## Option table

The table below is generated from the canonical option metadata so it stays in sync with the CLI and Stack Builder.

<OptionCategoryTable ecosystem="python" />
