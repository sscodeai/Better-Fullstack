---
title: Rust Options
description: Rust ecosystem option values for web frameworks, frontend, ORM, API, CLI, logging, services, and workflow flags.
---

Use these values with `--ecosystem rust` in CLI commands, MCP payloads, and Stack Builder links.

<Callout kind="info" title="Generated behavior">
Rust templates are Cargo-first. Framework, frontend, ORM, API, CLI, logging, error handling, libraries, services, and workflow flags are all validated against the selected stack.
</Callout>

## Option table

The table below is generated from the canonical option metadata so it stays in sync with the CLI and Stack Builder.

<OptionCategoryTable ecosystem="rust" />
