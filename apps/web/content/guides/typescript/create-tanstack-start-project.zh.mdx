---
title: 创建 TanStack Start 项目
description: 使用 Better Fullstack 创建包含 routing、API routes、Drizzle、SQLite、Better Auth、Tailwind CSS 和 shadcn/ui 的 TanStack Start fullstack app。
updated: 2026-05-12
category: TypeScript
tags:
  - tanstack-start
  - react
  - drizzle
  - better-auth
keywords:
  - create tanstack start project
  - tanstack start starter
  - tanstack start boilerplate
  - tanstack start fullstack app
  - tanstack start drizzle
---

当你想要围绕 TanStack Router 和 TanStack Start server route model 构建 React fullstack app 时，使用这个 stack。

```npm
npm create better-fullstack@latest my-tanstack-app -- \
  --ecosystem typescript \
  --frontend tanstack-start \
  --backend self \
  --database sqlite \
  --orm drizzle \
  --auth better-auth \
  --api trpc \
  --css-framework tailwind \
  --ui-library shadcn-ui \
```

## 创建内容

- 带 file-based routes 的 TanStack Start web app。
- 通过 framework 内置 server layer 实现 fullstack routing。
- SQLite 和 Drizzle，用于小型本地关系型数据库设置。
- 已接入 Better Auth。
- frontend 使用 Tailwind CSS 和 shadcn/ui。
- `bts.jsonc` 中保存可复现的 stack 配置。

## 生成结构

生成的项目是一个单独可部署的 web app，而不是拆开的 frontend/API workspace。TanStack Start 负责 routing、server functions 和 request handling；Drizzle 负责 database schema 和 queries。

代表性结构：

```txt
my-tanstack-app/
  bts.jsonc
  package.json
  src/
    routes/
    components/
    lib/
      auth/
      db/
      trpc/
```

准确文件名可能随 generator 演进，但边界保持稳定：route code 留在 TanStack Start app 中，database code 放在 Drizzle helpers 后面，API procedures 通过 `--api trpc` 暴露。

## Route 和 API 模式

典型 route 会把 UI state 留在 React 中，并调用 typed API helper，而不是手写 `fetch` URL。新增 API 时，应围绕 use case 建立窄 procedures，不要一开始就暴露表形 CRUD。

Drizzle 在 schema 小而明确时效果最好。Better Auth 拥有自己的 auth tables 和 session behavior；product models 应引用 user identifier，而不是复制 auth state。

## 何时选择

当你需要 React、typed routing、colocated fullstack behavior，并且想要比大型 batteries-included framework 更小的 framework surface 时，选择 TanStack Start。

它适合 dashboards、internal tools、SaaS prototypes，以及 route type safety 很重要的应用。

## 兼容性

- `--backend self` 是这里的正确选择，因为 TanStack Start 提供 app server layer。
- `--api trpc` 与这个 React stack 兼容，并提供 end-to-end typed calls。
- SQLite 适合 local development 和小型部署。需要 managed production databases、pooling、replicas 或更强运维能力时再迁移到 PostgreSQL。
- shadcn/ui 假设使用 Tailwind-based React UI。如果以后移除 Tailwind，请一起检查 generated component imports 和 class names。

## 部署

部署前，确保为 database、Better Auth secrets，以及 auth callbacks 使用的 public application URL 配好环境变量。对于 SQLite 部署，请确认 host 支持持久化可写存储；许多 serverless platforms 不支持。

如果目标是 serverless platform，PostgreSQL 通常比本地 SQLite 文件更适合作为生产数据库。提交 `bts.jsonc`，方便之后的 teammates 和 agents 还原预期 stack。

## 下一步

- 打开 [Stack Builder](/new?eco=typescript&fe-w=tanstack-start&be=self&db=sqlite&orm=drizzle&au=better-auth&api=trpc)。
- 阅读 [TypeScript ecosystem docs](/docs/ecosystems/typescript/)。
- 查看 [CLI create reference](/docs/cli/create/)。
