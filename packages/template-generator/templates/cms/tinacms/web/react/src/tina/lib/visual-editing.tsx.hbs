"use client";

import { useTina } from "tinacms/dist/react";

export function useVisualEditing<T extends object>(props: {
  query: string;
  variables: object;
  data: T;
}): T {
  const { data } = useTina(props);
  return data;
}
