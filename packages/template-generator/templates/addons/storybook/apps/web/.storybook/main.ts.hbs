import type { StorybookConfig } from "{{#if (includes frontend "next")}}@storybook/nextjs{{else if (or (includes frontend "tanstack-router") (includes frontend "react-router") (includes frontend "react-vite") (includes frontend "solid"))}}@storybook/react-vite{{else if (includes frontend "nuxt")}}@storybook/vue3-vite{{else if (includes frontend "svelte")}}@storybook/svelte-vite{{else}}@storybook/react-vite{{/if}}";

const config: StorybookConfig = {
  stories: ["../src/**/*.mdx", "../src/**/*.stories.@(js|jsx|mjs|ts|tsx)"],
  addons: [
    "@storybook/addon-essentials",
    "@storybook/addon-interactions",
  ],
  framework: {
    name: "{{#if (includes frontend "next")}}@storybook/nextjs{{else if (or (includes frontend "tanstack-router") (includes frontend "react-router") (includes frontend "react-vite") (includes frontend "solid"))}}@storybook/react-vite{{else if (includes frontend "nuxt")}}@storybook/vue3-vite{{else if (includes frontend "svelte")}}@storybook/svelte-vite{{else}}@storybook/react-vite{{/if}}",
    options: {},
  },
};

export default config;
